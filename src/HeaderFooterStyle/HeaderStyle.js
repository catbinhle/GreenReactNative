import { StyleSheet } from "react-native";

const HeaderStyle = StyleSheet.create({
    cityHeader: {
        alignItems: 'center',
        borderBottomWidth: 5,
        marginTop: 16
    },
    headerText: {
        fontWeight: 'bold',
        fontSize: 50,
        marginTop: 10,
        marginHorizontal: 16,
    },
})

export default HeaderStyle