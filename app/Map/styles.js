import { StyleSheet, Dimensions } from "react-native"

const Styles = StyleSheet.create({
    container: {
        flex: 1, 
        padding: 11  
    },
    map: {
        width: '100%',
        height: '100%'
    }
})

export default Styles